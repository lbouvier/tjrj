@model IEnumerable<TJRJ.ViewModels.AssuntoViewModel>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/AdminLTE/_Layout.cshtml";
}

<h1>Assuntos</h1>
<hr />

<p>
    <a asp-action="Create" class="btn btn-sm btn-success">Cadastrar</a>
</p>
<table class="table table-striped table-condensed table-bordered">
    <thead>

        <tr align="center">
            <th>@Html.DisplayNameFor(model => model.CodAs)</th>
            <th>
                @Html.DisplayNameFor(model => model.Descricao)
            </th>
            <th>Ações</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td align="center">@item.CodAs</td>
                <td align="center">
                    @Html.DisplayFor(modelItem => item.Descricao)
                </td>
                <td align="center">
                    <a asp-action="Edit" asp-route-id="@item.CodAs" class="btn btn-xs btn-info">Editar</a>
                    <a asp-action="Details" asp-route-id="@item.CodAs" class="btn btn-xs btn-warning">Detalhes</a>
                    <a asp-action="Delete" asp-route-id="@item.CodAs" class="btn btn-xs btn-danger">Excluir</a>
                </td>
            </tr>
        }
    </tbody>
</table>
